apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init-scripts
  namespace: orderflow
data:
  init-databases.sql: |
    -- 데이터베이스 생성
    CREATE DATABASE user_db;
    CREATE DATABASE product_db;
    CREATE DATABASE order_db;
    CREATE DATABASE payment_db;
    CREATE DATABASE inventory_db;
    CREATE DATABASE shipping_db;
    CREATE DATABASE notification_db;
    CREATE DATABASE event_logging_db;

    -- User DB 권한
    \c user_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Product DB 권한
    \c product_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Order DB 권한
    \c order_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Payment DB 권한
    \c payment_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Inventory DB 권한
    \c inventory_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Shipping DB 권한
    \c shipping_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Notification DB 권한
    \c notification_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- Event Logging DB 권한
    \c event_logging_db;
    GRANT ALL PRIVILEGES ON SCHEMA public TO orderflow;

    -- User Service Tables
    \c user_db;
    CREATE TABLE users (
                           id BIGSERIAL PRIMARY KEY,
                           email VARCHAR(100) NOT NULL,
                           provider_id VARCHAR(100) NOT NULL,
                           nickname VARCHAR(50),
                           login_type VARCHAR(20) NOT NULL,
                           role_type VARCHAR(20) NOT NULL DEFAULT 'ROLE_USER',
                           deleted BOOLEAN NOT NULL DEFAULT FALSE,
                           created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                           updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                           CONSTRAINT uq_user_login_provider UNIQUE (login_type, provider_id)
    );
    CREATE INDEX idx_user_email ON users(email);
    CREATE INDEX idx_user_role ON users(role_type);

    -- Product Service Tables
    \c product_db;
    CREATE TABLE products (
                              product_id VARCHAR(50) PRIMARY KEY,
                              product_name VARCHAR(255) NOT NULL,
                              description TEXT,
                              price DECIMAL(10, 2) NOT NULL,
                              category VARCHAR(50) NOT NULL,
                              deleted BOOLEAN NOT NULL DEFAULT FALSE,
                              created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                              updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_product_category ON products(category);
    CREATE INDEX idx_product_deleted ON products(deleted);

    -- Order Service Tables
    \c order_db;
    CREATE TABLE orders (
                            order_id VARCHAR(50) PRIMARY KEY,
                            user_id VARCHAR(50) NOT NULL,
                            total_price DECIMAL(12, 2) NOT NULL,
                            order_status VARCHAR(30) NOT NULL DEFAULT 'ORDER_CREATED',
                            shipping_address VARCHAR(500) NOT NULL,
                            shipping_city VARCHAR(100),
                            shipping_postal_code VARCHAR(20),
                            deleted BOOLEAN NOT NULL DEFAULT FALSE,
                            created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                            updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_order_user ON orders(user_id);
    CREATE INDEX idx_order_status ON orders(order_status);

    CREATE TABLE order_items (
                                 order_item_id VARCHAR(50) PRIMARY KEY,
                                 order_id VARCHAR(50) NOT NULL REFERENCES orders(order_id) ON DELETE CASCADE,
                                 product_id VARCHAR(50) NOT NULL,
                                 quantity INTEGER NOT NULL,
                                 unit_price DECIMAL(10, 2) NOT NULL,
                                 subtotal DECIMAL(12, 2) NOT NULL,
                                 deleted BOOLEAN NOT NULL DEFAULT FALSE,
                                 created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                                 updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_orderitem_order ON order_items(order_id);

    -- Payment Service Tables
    \c payment_db;
    CREATE TABLE payments (
                              payment_id VARCHAR(50) PRIMARY KEY,
                              order_id VARCHAR(50) NOT NULL,
                              user_id VARCHAR(50) NOT NULL,
                              amount DECIMAL(12, 2) NOT NULL,
                              payment_status VARCHAR(30) NOT NULL DEFAULT 'PAYMENT_PENDING',
                              payment_method VARCHAR(30),
                              transaction_id VARCHAR(100),
                              failure_reason VARCHAR(500),
                              deleted BOOLEAN NOT NULL DEFAULT FALSE,
                              created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                              updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_payment_order ON payments(order_id);
    CREATE INDEX idx_payment_status ON payments(payment_status);

    -- Inventory Service Tables
    \c inventory_db;
    CREATE TABLE inventory (
                               inventory_id VARCHAR(50) PRIMARY KEY,
                               product_id VARCHAR(50) NOT NULL UNIQUE,
                               total_stock INTEGER NOT NULL DEFAULT 0,
                               reserved_stock INTEGER NOT NULL DEFAULT 0,
                               available_stock INTEGER NOT NULL DEFAULT 0,
                               deleted BOOLEAN NOT NULL DEFAULT FALSE,
                               created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                               updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                               last_updated TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_inv_product ON inventory(product_id);

    CREATE TABLE inventory_logs (
                                    log_id VARCHAR(50) PRIMARY KEY,
                                    inventory_id VARCHAR(50) NOT NULL REFERENCES inventory(inventory_id) ON DELETE CASCADE,
                                    product_id VARCHAR(50) NOT NULL,
                                    action VARCHAR(30) NOT NULL,
                                    quantity INTEGER NOT NULL,
                                    reason VARCHAR(500),
                                    order_id VARCHAR(50),
                                    deleted BOOLEAN NOT NULL DEFAULT FALSE,
                                    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                                    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                                    timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_invlog_inventory ON inventory_logs(inventory_id);

    -- Shipping Service Tables
    \c shipping_db;
    CREATE TABLE shipments (
                               shipment_id VARCHAR(50) PRIMARY KEY,
                               order_id VARCHAR(50) NOT NULL UNIQUE,
                               user_id VARCHAR(50) NOT NULL,
                               shipping_status VARCHAR(30) NOT NULL DEFAULT 'SHIPPING_PREPARING',
                               tracking_number VARCHAR(100),
                               carrier VARCHAR(50),
                               estimated_delivery_date DATE,
                               actual_delivery_date TIMESTAMP,
                               deleted BOOLEAN NOT NULL DEFAULT FALSE,
                               created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                               updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_ship_order ON shipments(order_id);
    CREATE INDEX idx_ship_status ON shipments(shipping_status);

    -- Notification Service Tables
    \c notification_db;
    CREATE TABLE notifications (
                                   notification_id VARCHAR(50) PRIMARY KEY,
                                   user_id VARCHAR(50) NOT NULL,
                                   message VARCHAR(1000) NOT NULL,
                                   notification_type VARCHAR(30) NOT NULL,
                                   is_read BOOLEAN NOT NULL DEFAULT FALSE,
                                   related_order_id VARCHAR(50),
                                   read_at TIMESTAMP,
                                   deleted BOOLEAN NOT NULL DEFAULT FALSE,
                                   created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                                   updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_notif_user ON notifications(user_id);
    CREATE INDEX idx_notif_read ON notifications(is_read);

    -- Event Logging Service Tables
    \c event_logging_db;
    CREATE TABLE event_logs (
                                event_id VARCHAR(50) PRIMARY KEY,
                                order_id VARCHAR(50),
                                event_type VARCHAR(50) NOT NULL,
                                event_data TEXT,
                                service VARCHAR(50) NOT NULL,
                                status VARCHAR(20) NOT NULL DEFAULT 'SUCCESS',
                                deleted BOOLEAN NOT NULL DEFAULT FALSE,
                                created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
                                updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
    );
    CREATE INDEX idx_event_order ON event_logs(order_id);
    CREATE INDEX idx_event_type ON event_logs(event_type);

    \c postgres;
    SELECT 'OrderFlow DB 초기화 완료!' as message;