apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: orderflow
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
      name: client
    - port: 29092
      targetPort: 29092
      name: internal