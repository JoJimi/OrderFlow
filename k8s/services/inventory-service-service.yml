apiVersion: v1
kind: Service
metadata:
  namespace: orderflow
  name: inventory-service
  labels:
    part-of: orderflow
    component: backend-server
    name: inventory-service
    instance: orderflow-inventory-service
    version: "1.0.0"
    managed-by: kubectl
spec:
  selector:
    part-of: orderflow
    component: backend-server
    name: inventory-service
    instance: orderflow-inventory-service
  ports:
    - port: 8085
      targetPort: http
      name: http
  type: ClusterIP