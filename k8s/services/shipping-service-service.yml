apiVersion: v1
kind: Service
metadata:
  namespace: orderflow
  name: shipping-service
  labels:
    part-of: orderflow
    component: backend-server
    name: shipping-service
    instance: orderflow-shipping-service
    version: "1.0.0"
    managed-by: kubectl
spec:
  selector:
    part-of: orderflow
    component: backend-server
    name: shipping-service
    instance: orderflow-shipping-service
  ports:
    - port: 8086
      targetPort: http
      name: http
  type: ClusterIP