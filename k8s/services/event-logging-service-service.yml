apiVersion: v1
kind: Service
metadata:
  namespace: orderflow
  name: event-logging-service
  labels:
    part-of: orderflow
    component: backend-server
    name: event-logging-service
    instance: orderflow-event-logging-service
    version: "1.0.0"
    managed-by: kubectl
spec:
  selector:
    part-of: orderflow
    component: backend-server
    name: event-logging-service
    instance: orderflow-event-logging-service
  ports:
    - port: 8088
      targetPort: http
      name: http
  type: ClusterIP