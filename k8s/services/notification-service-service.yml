apiVersion: v1
kind: Service
metadata:
  namespace: orderflow
  name: notification-service
  labels:
    part-of: orderflow
    component: backend-server
    name: notification-service
    instance: orderflow-notification-service
    version: "1.0.0"
    managed-by: kubectl
spec:
  selector:
    part-of: orderflow
    component: backend-server
    name: notification-service
    instance: orderflow-notification-service
  ports:
    - port: 8087
      targetPort: http
      name: http
  type: ClusterIP
